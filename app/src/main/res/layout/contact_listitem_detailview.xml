<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="contact"
            type="org.dieschnittstelle.mobile.android.skeleton.model.Contacts" />
        <variable
            name="viewmodel"
            type="org.dieschnittstelle.mobile.android.skeleton.viewmodel.DetailviewViewmodelImpl" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/contact_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{contact.name}"/>

        <ImageButton
            android:id="@+id/delete_contact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:src="@drawable/delete_icon"
            android:contentDescription="Delete contact from ToDo"
            android:textSize="10sp"
            android:onClick="@{() -> viewmodel.onContactDeleteButtonClicked()}"/>

    </LinearLayout>
</layout>